
<header class="<%= current_page?(root_path) ? "hero" : "hero small" %>">
  <section class="title">
   
  </section>
</header>
<nav class="navbar navbar-expand-lg navbar-dark sticky-nav">
  <div class="container">
    <a class="navbar-brand" href="<%= root_path %>">Find My Shelter</a>
    
    
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">  <%= link_to 'About', :about, class: 'nav-link px-2' %> </li>
        <li class="nav-item"> <%= link_to 'Admin', [:admin, :root], class: 'nav-link px-2' %> </li>
      </ul>
        


        <ul class="nav justify-content-center border-bottom pb-3 mb-3">
      
    </ul>
     </div>
  </div>
</nav>
<% if current_user %>
     <span> Signed in as <%= current_user.first_name %> &nbsp | &nbsp <%= link_to "Logout", '/logout', class: 'text-muted' %> </span
    <% else %>
     <%= link_to 'Login', '/login',  class: 'text-muted' %> &nbsp | &nbsp <%= link_to 'Signup', '/signup', class: 'text-muted' %>
    <% end %>
<script>
(function(){
const gnr = () => {
    const uptitle = document.querySelector("section.title h3");
    const subtitle = document.querySelector("section.title h2");
    const header = document.querySelector("header.hero");

    uptitle.style.fontSize = "5em";
    uptitle.style.fontWeight = 900;
    uptitle.style.textAlign = "center";

    subtitle.innerHTML =
      "<p>Watch it bring you to your</p><p> shu-n-n-n-n-n-n-n-n knees, knees!</p>";
  };
  const title = document.querySelector("header.hero");
  title.addEventListener("dblclick", gnr);
  
})()
  </script>